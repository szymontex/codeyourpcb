---
phase: 02-rendering
plan: 09
type: execute
wave: 2
depends_on: ["02-08"]
files_modified:
  - viewer/src/wasm.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "TypeScript can import and instantiate real PcbEngine from WASM"
    - "Real WASM PcbEngine replaces MockPcbEngine"
  artifacts:
    - path: "viewer/src/wasm.ts"
      provides: "Dynamic WASM import enabled"
      contains: "import.*pkg/cypcb_render"
  key_links:
    - from: "viewer/src/wasm.ts"
      to: "viewer/pkg/cypcb_render.js"
      via: "dynamic import"
      pattern: "await import.*cypcb_render"
---

<objective>
Enable real WASM integration by uncommenting the WASM import code in wasm.ts.

Purpose: With the WASM build now working (from 02-08), the viewer can use the real Rust-based PcbEngine instead of the JavaScript MockPcbEngine. This completes the WASM integration gap.

Output: viewer/src/wasm.ts uses real WASM module with MockPcbEngine as fallback only when WASM is unavailable.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rendering/02-08-SUMMARY.md

# Key files
@viewer/src/wasm.ts
@viewer/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable real WASM import in wasm.ts</name>
  <files>
    viewer/src/wasm.ts
  </files>
  <action>
In viewer/src/wasm.ts, the WASM import code is currently commented out (lines 276-286). Enable it:

1. Uncomment the try/catch block that dynamically imports the WASM module:
```typescript
try {
  const wasmPath = '../pkg/cypcb_render.js';
  const wasm = await import(/* @vite-ignore */ wasmPath);
  await wasm.default();
  wasmModule = wasm;
  engineInstance = new wasm.PcbEngine() as PcbEngine;
  console.log('WASM module loaded successfully');
  return engineInstance;
} catch (e) {
  console.log('WASM not available, using mock:', e);
}
```

2. Update the fallback log message to be more informative:
```typescript
console.log('Using MockPcbEngine (WASM fallback)');
```

3. Remove or update the comment that says "WASM build is currently blocked" - it's now working.

4. The MockPcbEngine class should remain as fallback for:
   - Development without WASM build
   - Environments where WASM fails to load
   - Testing without the Rust backend
  </action>
  <verify>
Run: `cd viewer && npx tsc --noEmit`

TypeScript should compile without errors.
  </verify>
  <done>
WASM import is uncommented and TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test WASM loading in browser</name>
  <files>
    viewer/src/main.ts
    viewer/src/wasm.ts
  </files>
  <action>
Verify the real WASM module loads in the browser:

1. Start the dev server: `cd viewer && npm run dev`
   (Use the plain dev server without the watch server for this test)

2. Open browser to http://localhost:5173

3. Check browser console for:
   - "WASM module loaded successfully" (indicates real WASM loaded)
   - OR "WASM not available, using mock" (indicates fallback)

4. If WASM loads but there are runtime errors, diagnose:
   - Check if the PcbEngine methods match the interface
   - Check if the snapshot format matches TypeScript types
   - Look for WASM memory or initialization errors

5. If there are type mismatches between Rust and TypeScript:
   - Compare BoardSnapshot fields in snapshot.rs vs types.ts
   - Ensure all fields use the same names and types
   - snake_case in Rust should match snake_case in TypeScript (serde defaults to snake_case)
  </action>
  <verify>
Open http://localhost:5173 and check browser console.

Should show "WASM module loaded successfully".
  </verify>
  <done>
Browser loads WASM module successfully, board renders using real Rust engine.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify full functionality with real WASM</name>
  <files>
    viewer/
  </files>
  <action>
With the real WASM engine loaded, verify all Phase 2 features work:

1. Start the hot reload server: `npm run dev:watch`

2. Open http://localhost:5173

3. Verify:
   - Board outline renders (yellow rectangle)
   - Component pads render (red for top copper)
   - Zoom works (scroll wheel zooms at cursor)
   - Pan works (middle-click drag)
   - Selection works (left-click highlights component orange)
   - Layer toggles work (unchecking Top hides top copper pads)

4. Test hot reload:
   - Edit examples/blink.cypcb (change a position value)
   - Save the file
   - Verify the viewer updates within 500ms
   - Verify viewport is preserved (same zoom/pan position)
   - Verify "Reloaded" message appears

5. Compare behavior with MockPcbEngine to ensure parity:
   - The real WASM engine should produce identical rendering
   - Any differences indicate type/data format issues

If any feature doesn't work with real WASM but worked with Mock, the issue is in the Rust -> JS data transfer. Debug by comparing get_snapshot() output.
  </action>
  <verify>
All Phase 2 features work with real WASM engine:
- Board renders correctly
- Navigation works (zoom, pan)
- Selection works
- Layer toggles work
- Hot reload works
  </verify>
  <done>
Full Phase 2 functionality verified with real WASM engine, matching Mock behavior.
  </done>
</task>

</tasks>

<verification>
## Phase Gap Closure Verification

After completing all tasks:

1. **WASM Import**: wasm.ts has uncommented dynamic import
2. **Browser Console**: Shows "WASM module loaded successfully"
3. **Rendering**: Board renders identically to Mock implementation
4. **Interaction**: All zoom/pan/select features work
5. **Hot Reload**: File changes trigger re-render with viewport preservation
</verification>

<success_criteria>
- TypeScript can import and instantiate real PcbEngine from WASM (gap #2 from VERIFICATION.md closed)
- Real WASM engine renders board correctly
- All Phase 2 functionality works with real engine
- MockPcbEngine remains as working fallback
</success_criteria>

<output>
After completion, create `.planning/phases/02-rendering/02-09-SUMMARY.md`
</output>
