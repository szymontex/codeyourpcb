---
phase: 03-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cypcb-drc/Cargo.toml
  - crates/cypcb-drc/src/lib.rs
  - crates/cypcb-drc/src/violation.rs
  - crates/cypcb-drc/src/rules/mod.rs
  - Cargo.toml
autonomous: true

must_haves:
  truths:
    - "DRC crate compiles with all dependencies"
    - "DrcViolation type captures location, entities, and message"
    - "DrcRule trait defines check() interface"
  artifacts:
    - path: "crates/cypcb-drc/Cargo.toml"
      provides: "DRC crate manifest"
      contains: "cypcb-drc"
    - path: "crates/cypcb-drc/src/violation.rs"
      provides: "DrcViolation and ViolationKind types"
      exports: ["DrcViolation", "ViolationKind"]
    - path: "crates/cypcb-drc/src/rules/mod.rs"
      provides: "DrcRule trait definition"
      exports: ["DrcRule"]
  key_links:
    - from: "crates/cypcb-drc/src/lib.rs"
      to: "crates/cypcb-drc/src/violation.rs"
      via: "pub use"
      pattern: "pub use violation"
---

<objective>
Create the cypcb-drc crate with foundational types for Design Rule Checking.

Purpose: Establish the DRC infrastructure that all rule implementations will use. This is the foundation for all validation work in Phase 3.

Output: A new cypcb-drc crate with DrcViolation type, ViolationKind enum, and DrcRule trait.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-validation/03-CONTEXT.md
@.planning/phases/03-validation/03-RESEARCH.md

# Existing types to reference
@crates/cypcb-core/src/lib.rs
@crates/cypcb-parser/src/ast.rs (Span type)
@crates/cypcb-world/src/spatial.rs (SpatialEntry for reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cypcb-drc crate structure</name>
  <files>
    crates/cypcb-drc/Cargo.toml
    crates/cypcb-drc/src/lib.rs
    Cargo.toml
  </files>
  <action>
Create the cypcb-drc crate:

1. Create `crates/cypcb-drc/Cargo.toml`:
   - name = "cypcb-drc"
   - Dependencies: cypcb-core, cypcb-world (path), thiserror, miette, hashbrown = "0.15"
   - Optional: rayon = { version = "1.10", optional = true }
   - Features: default = [], parallel = ["rayon"]
   - Use workspace for common deps (thiserror, miette)

2. Create `crates/cypcb-drc/src/lib.rs`:
   - Module declarations: mod violation; mod rules;
   - Re-exports: pub use violation::{DrcViolation, ViolationKind};
   - Re-export DrcRule trait from rules
   - Add DrcResult struct with:
     - violations: Vec<DrcViolation>
     - duration_ms: u64
     - passed() -> bool method
     - violation_count() -> usize method
   - Stub run_drc(world: &BoardWorld, rules: &DesignRules) -> DrcResult function (returns empty result for now)

3. Add cypcb-drc to workspace Cargo.toml dependencies section.

Reference 03-RESEARCH.md Pattern 1 (DRC Rule as Trait) for API design.
  </action>
  <verify>
    cd /workspace/codeyourpcb && cargo check -p cypcb-drc
  </verify>
  <done>cypcb-drc crate exists and compiles with stub implementation</done>
</task>

<task type="auto">
  <name>Task 2: Implement DrcViolation and ViolationKind types</name>
  <files>crates/cypcb-drc/src/violation.rs</files>
  <action>
Create `crates/cypcb-drc/src/violation.rs` with:

1. ViolationKind enum with variants:
   - Clearance - clearance between copper features too small
   - TraceWidth - trace width below minimum (placeholder for future)
   - DrillSize - drill hole below minimum
   - UnconnectedPin - pin has no net connection
   - ViaDrill - via drill below minimum (placeholder)
   - AnnularRing - annular ring below minimum (placeholder)
   Derive: Debug, Clone, Copy, PartialEq, Eq

2. DrcViolation struct with fields:
   - kind: ViolationKind
   - location: Point (board position for click-to-zoom)
   - entity: Entity (primary entity involved)
   - other_entity: Option<Entity> (secondary for clearance)
   - source_span: Option<Span> (for error reporting in DSL)
   - message: String (human-readable description)
   Derive: Debug, Clone

3. Constructor methods on DrcViolation:
   - clearance(entity, other, actual: Nm, required: Nm, location: Point) -> Self
   - drill_size(entity, actual: Nm, required: Nm, location: Point) -> Self
   - unconnected_pin(entity, pin: &str, refdes: &str, location: Point) -> Self

Use cypcb_core::{Nm, Point} and bevy_ecs::entity::Entity.
Reference 03-RESEARCH.md Pattern 3 for field design.
  </action>
  <verify>
    cd /workspace/codeyourpcb && cargo test -p cypcb-drc
  </verify>
  <done>DrcViolation type with constructors for all violation kinds</done>
</task>

<task type="auto">
  <name>Task 3: Define DrcRule trait</name>
  <files>crates/cypcb-drc/src/rules/mod.rs</files>
  <action>
Create `crates/cypcb-drc/src/rules/mod.rs` with:

1. DrcRule trait:
   ```rust
   pub trait DrcRule: Send + Sync {
       /// Rule identifier for error messages
       fn name(&self) -> &'static str;

       /// Execute rule check against board world
       fn check(
           &self,
           world: &BoardWorld,
           rules: &DesignRules,
       ) -> Vec<DrcViolation>;
   }
   ```

2. Placeholder DesignRules struct (will be fleshed out in 03-02):
   ```rust
   #[derive(Debug, Clone, Default)]
   pub struct DesignRules {
       pub min_clearance: Nm,
       pub min_trace_width: Nm,
       pub min_drill_size: Nm,
   }
   ```

3. Rule struct placeholders (empty impls, will be implemented in later plans):
   - pub struct ClearanceRule;
   - pub struct MinDrillSizeRule;
   - pub struct UnconnectedPinRule;

4. Add unit test verifying trait is object-safe:
   ```rust
   #[test]
   fn test_trait_object_safe() {
       fn _assert_object_safe(_: &dyn DrcRule) {}
   }
   ```

Use cypcb_world::BoardWorld and crate::DrcViolation.
  </action>
  <verify>
    cd /workspace/codeyourpcb && cargo test -p cypcb-drc -- --include-ignored
  </verify>
  <done>DrcRule trait defined and is object-safe</done>
</task>

</tasks>

<verification>
All checks pass:
- `cargo check -p cypcb-drc` compiles
- `cargo test -p cypcb-drc` all tests pass
- DrcViolation constructors work correctly
- DrcRule trait is object-safe
</verification>

<success_criteria>
1. cypcb-drc crate exists in workspace
2. DrcViolation captures: kind, location, entities, source span, message
3. ViolationKind has variants for all planned rule types
4. DrcRule trait compiles and is object-safe
5. DrcResult struct with passed() and violation_count() methods
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation/03-01-SUMMARY.md`
</output>
