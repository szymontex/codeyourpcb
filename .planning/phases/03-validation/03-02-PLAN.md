---
phase: 03-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cypcb-world/src/footprint/gullwing.rs
  - crates/cypcb-world/src/footprint/mod.rs
  - crates/cypcb-world/src/footprint/library.rs
autonomous: true

must_haves:
  truths:
    - "SOIC-8 and SOIC-14 footprints available in library"
    - "SOT-23 and SOT-23-5 footprints available in library"
    - "TQFP-32 footprint available in library"
    - "Footprints have correct pin counts and IPC-7351B dimensions"
  artifacts:
    - path: "crates/cypcb-world/src/footprint/gullwing.rs"
      provides: "Gull-wing IC footprint generators"
      exports: ["soic8", "soic14", "sot23", "sot23_5", "tqfp32"]
      min_lines: 150
  key_links:
    - from: "crates/cypcb-world/src/footprint/library.rs"
      to: "crates/cypcb-world/src/footprint/gullwing.rs"
      via: "register_builtin_gullwing"
      pattern: "gullwing::"
---

<objective>
Add QFP/SOIC/SOT footprint families to the footprint library.

Purpose: Fulfill FTP-03 requirement for IC package support. These are essential for real-world PCB designs with microcontrollers and other ICs.

Output: Gull-wing footprint generators for SOIC, SOT, and QFP packages registered in FootprintLibrary.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-validation/03-RESEARCH.md

# Existing footprint infrastructure
@crates/cypcb-world/src/footprint/library.rs
@crates/cypcb-world/src/footprint/smd.rs
@crates/cypcb-world/src/footprint/tht.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gullwing.rs with SOIC footprints</name>
  <files>
    crates/cypcb-world/src/footprint/gullwing.rs
    crates/cypcb-world/src/footprint/mod.rs
  </files>
  <action>
Create `crates/cypcb-world/src/footprint/gullwing.rs` with:

1. Parametric `gullwing_footprint()` helper function:
   - Parameters: name, description, pin_count, pitch, pad_width, pad_length, row_span, body_size
   - Pin numbering: counter-clockwise from bottom-left (IC standard)
   - Handles dual-row packages (left side pins 1..n/2, right side pins n..n/2+1)
   - Auto-calculates courtyard: body + 0.5mm margin (IPC-7351B)
   - SMD layers: TopCopper, TopPaste, TopMask

2. `soic8()` function:
   - 8 pins, 1.27mm pitch
   - Pad: 0.6mm wide, 1.5mm long
   - Row span: 5.4mm (center to center)
   - Body: 5.0mm x 4.0mm
   - Description: "Small Outline IC, 8 pins, 1.27mm pitch"

3. `soic14()` function:
   - 14 pins, 1.27mm pitch
   - Same pad dimensions as SOIC-8
   - Body: 5.0mm x 8.7mm

Reference 03-RESEARCH.md Pattern 5 for exact implementation.

4. Add `pub mod gullwing;` to footprint/mod.rs.
  </action>
  <verify>
    cd /workspace/codeyourpcb && cargo test -p cypcb-world footprint::gullwing
  </verify>
  <done>SOIC-8 and SOIC-14 footprints compile with correct dimensions</done>
</task>

<task type="auto">
  <name>Task 2: Add SOT and TQFP footprints</name>
  <files>crates/cypcb-world/src/footprint/gullwing.rs</files>
  <action>
Add to gullwing.rs:

1. `sot23()` function (3-pin, asymmetric):
   - Special layout: 2 pins on one side, 1 on the other
   - Pin 1: left side at (-0.95mm, -1.0mm)
   - Pin 2: right side at (0.95mm, -1.0mm)
   - Pin 3: top center at (0mm, 1.0mm)
   - Pad size: 0.6mm x 1.0mm
   - Body: 3.0mm x 2.5mm
   - Courtyard: 3.5mm x 3.0mm

2. `sot23_5()` function (5-pin):
   - Pins 1-3 on left side (0.95mm pitch)
   - Pins 4-5 on right side
   - Pad size: 1.0mm x 0.6mm (horizontal)
   - Row span: 2.4mm (center-to-center)

3. `tqfp32()` function (quad-flat, 4 sides):
   - 32 pins (8 per side), 0.8mm pitch
   - Pad: 0.45mm wide, 1.5mm long
   - Body: 7.0mm x 7.0mm
   - Row span: 9.0mm
   - Pins numbered counter-clockwise: bottom 1-8, right 9-16, top 17-24, left 25-32

Reference 03-RESEARCH.md for exact SOT-23 and TQFP-32 implementations.
  </action>
  <verify>
    cd /workspace/codeyourpcb && cargo test -p cypcb-world footprint
  </verify>
  <done>SOT-23, SOT-23-5, and TQFP-32 footprints with correct pin counts</done>
</task>

<task type="auto">
  <name>Task 3: Register gullwing footprints in library</name>
  <files>crates/cypcb-world/src/footprint/library.rs</files>
  <action>
Update FootprintLibrary to include gullwing footprints:

1. Add `register_builtin_gullwing()` method to FootprintLibrary:
   ```rust
   fn register_builtin_gullwing(&mut self) {
       use super::gullwing::*;
       self.register(soic8());
       self.register(soic14());
       self.register(sot23());
       self.register(sot23_5());
       self.register(tqfp32());
   }
   ```

2. Call `register_builtin_gullwing()` from `FootprintLibrary::new()` after existing registrations.

3. Add unit tests:
   - Test SOIC-8 has 8 pads
   - Test SOT-23 has 3 pads
   - Test TQFP-32 has 32 pads
   - Test pin numbers are correct (first and last pad numbers)
   - Test library contains new footprints
  </action>
  <verify>
    cd /workspace/codeyourpcb && cargo test -p cypcb-world footprint
  </verify>
  <done>All IC footprints registered in library and accessible via get()</done>
</task>

</tasks>

<verification>
All checks pass:
- `cargo test -p cypcb-world` all tests pass
- FootprintLibrary::new().get("SOIC-8") returns footprint with 8 pads
- FootprintLibrary::new().get("SOT-23") returns footprint with 3 pads
- FootprintLibrary::new().get("TQFP-32") returns footprint with 32 pads
- All pin positions are within expected bounds
</verification>

<success_criteria>
1. SOIC-8 and SOIC-14 footprints with 1.27mm pitch
2. SOT-23 (3-pin) and SOT-23-5 (5-pin) footprints
3. TQFP-32 footprint with 0.8mm pitch
4. All footprints registered in FootprintLibrary
5. IPC-7351B courtyard calculation (body + 0.5mm margin)
6. Counter-clockwise pin numbering from bottom-left
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation/03-02-SUMMARY.md`
</output>
