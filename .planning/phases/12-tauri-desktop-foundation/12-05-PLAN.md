---
phase: 12-tauri-desktop-foundation
plan: 05
type: execute
wave: 4
depends_on: ['12-03']
files_modified: [viewer/src/main.ts]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "File > Open loads .cypcb content into the viewer engine"
    - "File > Save retrieves current source content from the engine"
    - "View > Zoom In/Out/Fit adjusts the viewport"
    - "View > Toggle Theme cycles the theme"
    - "File > New clears the current design"
  artifacts:
    - path: "viewer/src/main.ts"
      provides: "Event listeners for all desktop custom events"
      contains: "desktop:open-file"
  key_links:
    - from: "viewer/src/desktop.ts"
      to: "viewer/src/main.ts"
      via: "CustomEvent dispatch/listen"
      pattern: "desktop:(open-file|content-request|viewport|toggle-theme|new-file)"
---

<objective>
Wire desktop menu events to the viewer engine by adding event listeners in main.ts.

Purpose: desktop.ts dispatches custom events (desktop:open-file, desktop:content-request, desktop:viewport, desktop:toggle-theme, desktop:new-file) but main.ts has no listeners for them. Without these listeners, native menu actions have no effect on the viewer.

Output: main.ts handles all desktop custom events, completing the end-to-end menu-to-viewer pipeline.
</objective>

<execution_context>
@/config/.claude/get-shit-done/workflows/execute-plan.md
@/config/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@viewer/src/main.ts
@viewer/src/desktop.ts
@viewer/src/viewport.ts
@viewer/src/theme/theme-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add desktop event listeners to main.ts</name>
  <files>viewer/src/main.ts</files>
  <action>
Add desktop event listeners inside the init() function, AFTER the engine is loaded and AFTER the desktop initialization block (after line 738). Place them inside a guard: `if (isDesktop())` to avoid registering listeners in web mode.

Add these 5 event listeners:

1. **desktop:open-file** - Receives `{ path: string, content: string }` in event.detail.
   - Call `engine.load_source(detail.content)` to parse the content
   - Update `snapshot = engine.get_snapshot()`
   - Update error badge via `updateErrorBadge(snapshot.violations || [])` (AFTER snapshot is assigned)
   - If snapshot.board exists, call `viewport = fitBoard(viewport, snapshot.board.width_nm, snapshot.board.height_nm)` and update `interactionState.viewport = viewport`
   - Set `currentFilePath = detail.path` (the local variable, for routing)
   - Update statusText with filename (extract from path using split)
   - Set `dirty = true`

2. **desktop:content-request** - No detail payload.
   - This is desktop.ts asking for the current source to save.
   - The engine does NOT have a get_source() method. Instead, there is no way to retrieve the original source from the engine after loading.
   - IMPORTANT: Track the last-loaded source content in a module-level variable `lastLoadedSource`. Set it in reload(), handleFileLoad(), and the desktop:open-file handler.
   - Respond by dispatching `desktop:content-response` with `{ content: lastLoadedSource }`:
     ```
     window.dispatchEvent(new CustomEvent('desktop:content-response', {
       detail: { content: lastLoadedSource }
     }));
     ```

3. **desktop:viewport** - Receives `{ action: 'zoom-in' | 'zoom-out' | 'fit' }` in event.detail.
   - For 'zoom-in': multiply viewport.scale by 1.5, update viewport and interactionState.viewport, set dirty = true
   - For 'zoom-out': divide viewport.scale by 1.5 (i.e., multiply by 0.6667), update viewport and interactionState.viewport, set dirty = true
   - For 'fit': if snapshot?.board exists, call `fitBoard(viewport, snapshot.board.width_nm, snapshot.board.height_nm)`, update viewport and interactionState.viewport, set dirty = true

4. **desktop:toggle-theme** - No detail payload.
   - Cycle theme exactly like the themeToggle click handler: get current from themeManager.getTheme(), compute next (light->dark->auto->light), call themeManager.setTheme(next), call updateThemeIcon() (the existing function defined at ~line 244 in main.ts, accessible within init() scope).

5. **desktop:new-file** - No detail payload.
   - Call `engine.load_source('')` to clear
   - Update `snapshot = engine.get_snapshot()`
   - Set `currentFilePath = null`
   - Set `lastLoadedSource = null`
   - Set statusText using the existing `usingWasm` variable: `statusText = usingWasm ? 'Ready (WASM) - Open a file' : 'Ready (Mock) - Open a file'`
   - Set `dirty = true`

Also add `let lastLoadedSource: string | null = null;` near the top of init() alongside the other state variables.

Set `lastLoadedSource = content` in:
- The reload() function (after engine.load_source succeeds)
- The handleFileLoad() function (after engine.load_source succeeds, only for .cypcb)
- The desktop:open-file handler
  </action>
  <verify>
Run `npx tsc --noEmit` from the viewer/ directory. TypeScript compilation must pass with no errors related to the new event listeners. Grep for "desktop:" in main.ts to confirm all 5 event types are handled.
  </verify>
  <done>
All 5 desktop custom events (open-file, content-request, viewport, toggle-theme, new-file) have listeners in main.ts. The lastLoadedSource variable tracks source content for save operations. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "desktop:" viewer/src/main.ts` returns at least 5 (one per event type)
2. `grep -n "lastLoadedSource\s*=" viewer/src/main.ts` shows assignments in at least 4 locations: declaration, reload() (~line 483), handleFileLoad() (~line 317), and desktop:open-file handler
3. `cd viewer && npx tsc --noEmit` passes
</verification>

<success_criteria>
- All 5 desktop custom events have listeners in main.ts
- desktop:open-file loads content into engine and updates viewport
- desktop:content-request responds with current source via desktop:content-response
- desktop:viewport handles zoom-in, zoom-out, and fit actions
- desktop:toggle-theme cycles the theme
- desktop:new-file clears the design
- lastLoadedSource is tracked across all content-loading code paths
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-tauri-desktop-foundation/12-05-SUMMARY.md`
</output>
