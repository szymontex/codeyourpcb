---
phase: 07-navigation-controls
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Ctrl+click and drag pans the viewport"
    - "Touchpad two-finger drag pans the viewport"
    - "Pinch-to-zoom works on touchpad"
    - "Existing middle-click pan still works"
    - "Works across Chrome, Firefox, Safari"
  artifacts: []
  key_links: []
---

<objective>
Verify all navigation controls work correctly across browsers.

Purpose: Phase 7 success criteria require cross-browser verification of all navigation methods. This checkpoint confirms no regressions and new touchpad support works.

Output: Human verification that all navigation controls function correctly.
</objective>

<execution_context>
@/config/.claude/get-shit-done/workflows/execute-plan.md
@/config/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-navigation-controls/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for testing</name>
  <files></files>
  <action>
Start the viewer dev server so the user can test navigation controls:
```bash
cd /workspace/codeyourpcb/viewer && npm run start
```
Wait for Vite to report the local URL (http://localhost:5173).
Instruct user to open the URL and load a .cypcb file from examples/ via the Open button.
  </action>
  <verify>Dev server is running and accessible at http://localhost:5173</verify>
  <done>Dev server running, ready for manual testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Navigation controls for laptop/touchpad users (Phase 7)</what-built>
  <how-to-verify>
    Open http://localhost:5173 in browser. Click "Open" and load examples/blink.cypcb.

    Test each navigation method:

    1. **Middle-click pan** (if available): Hold middle mouse button and drag. Board should pan smoothly.

    2. **Ctrl+LMB pan**: Hold Ctrl, click and drag with left mouse button. Board should pan smoothly.

    3. **Scroll wheel zoom**: Scroll up to zoom in, down to zoom out. Zoom centers on cursor position.

    4. **Touchpad two-finger pan** (if on laptop): Place two fingers on touchpad and drag. Board should pan. If no touchpad available, skip this test.

    5. **Touchpad pinch-to-zoom** (if on laptop): Pinch two fingers on touchpad to zoom in/out. If no touchpad, skip.

    6. **Left-click selection**: Click on a component. Status bar should show selected component name.

    7. **No regressions**: Verify all above work without interfering with each other.

    Test in Chrome first. If possible, also verify in Firefox and Safari.
  </how-to-verify>
  <resume-signal>Type "approved" if all navigation works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All Phase 7 success criteria verified by human:
1. Ctrl+click and drag pans the viewport
2. Touchpad gestures work for pan/zoom
3. Existing middle-click pan still works
4. Works across Chrome, Firefox, Safari
</verification>

<success_criteria>
- Human confirms all navigation methods work
- No regressions in existing interactions
- Cross-browser compatibility verified (at least Chrome)
</success_criteria>

<output>
After completion, create `.planning/phases/07-navigation-controls/07-02-SUMMARY.md`
</output>
