---
phase: 05-intelligence
plan: 08
type: execute
wave: 4
depends_on: ["05-06"]
files_modified:
  - crates/cypcb-render/src/snapshot.rs
  - crates/cypcb-render/src/lib.rs
  - viewer/src/types.ts
  - viewer/src/renderer.ts
  - viewer/src/layers.ts
autonomous: true

must_haves:
  truths:
    - "Traces render with actual width on copper layers"
    - "Vias render as filled circles with drill"
    - "Ratsnest shows unrouted connections"
    - "Ratsnest can be toggled in layer controls"
  artifacts:
    - path: "crates/cypcb-render/src/snapshot.rs"
      provides: "TraceInfo and RatsnestInfo types"
      contains: "TraceInfo"
    - path: "viewer/src/renderer.ts"
      provides: "Trace and ratsnest rendering"
      contains: "drawTrace|drawRatsnest"
  key_links:
    - from: "viewer/src/renderer.ts"
      to: "viewer/src/types.ts"
      via: "Uses TraceInfo and RatsnestInfo"
      pattern: "TraceInfo|RatsnestInfo"
---

<objective>
Add trace and ratsnest rendering to the viewer so routing results are visible.

Purpose: Users need to see traces after autorouting. Ratsnest shows what still needs routing. Per CONTEXT.md: "Full trace rendering: actual width, copper layer colors, vias visible" and "Ratsnest: toggle option in layer controls".
Output: Traces and vias visible in viewer, ratsnest toggleable
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-intelligence/05-CONTEXT.md
@crates/cypcb-render/src/snapshot.rs
@viewer/src/renderer.ts
@viewer/src/layers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BoardSnapshot with Traces</name>
  <files>
    crates/cypcb-render/src/snapshot.rs
    crates/cypcb-render/src/lib.rs
  </files>
  <action>
Add trace and ratsnest data to BoardSnapshot:

1. **TraceInfo struct**:
   - segments: Vec<TraceSegmentInfo>
   - width: f64 (nm as f64 for JS)
   - layer: String
   - net_name: String
   - locked: bool

2. **TraceSegmentInfo struct**:
   - start_x: f64, start_y: f64
   - end_x: f64, end_y: f64

3. **ViaInfo struct**:
   - x: f64, y: f64
   - drill: f64
   - outer_diameter: f64
   - net_name: String

4. **RatsnestInfo struct**:
   - start_x: f64, start_y: f64
   - end_x: f64, end_y: f64
   - net_name: String

5. **Update BoardSnapshot**:
   - traces: Vec<TraceInfo>
   - vias: Vec<ViaInfo>
   - ratsnest: Vec<RatsnestInfo>

6. **Update PcbEngine**:
   - collect_traces(): Query Trace entities, convert to TraceInfo
   - collect_vias(): Query Via entities, convert to ViaInfo
   - collect_ratsnest(): For each net, find unconnected pin pairs
     - Simple approach: if net has pins but no trace connecting them

7. **Ratsnest calculation**:
   - For each net, get all connected pins
   - Build minimum spanning tree (or just all pairs for simplicity)
   - For pins without trace between them, add ratsnest line

8. **Tests**:
   - Snapshot with traces
   - Snapshot with vias
   - Ratsnest for unrouted net
  </action>
  <verify>
    cargo test -p cypcb-render trace
    cargo test -p cypcb-render snapshot
  </verify>
  <done>
    TraceInfo and ViaInfo in snapshot.
    Ratsnest calculated for unrouted connections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Trace and Via Rendering</name>
  <files>
    viewer/src/types.ts
    viewer/src/renderer.ts
  </files>
  <action>
Add trace and via rendering to the Canvas renderer:

1. **Update types.ts**:
   ```typescript
   interface TraceSegmentInfo {
     start_x: number;
     start_y: number;
     end_x: number;
     end_y: number;
   }

   interface TraceInfo {
     segments: TraceSegmentInfo[];
     width: number;
     layer: string;
     net_name: string;
     locked: boolean;
   }

   interface ViaInfo {
     x: number;
     y: number;
     drill: number;
     outer_diameter: number;
     net_name: string;
   }

   interface RatsnestInfo {
     start_x: number;
     start_y: number;
     end_x: number;
     end_y: number;
     net_name: string;
   }
   ```

2. **Update BoardSnapshot interface** with traces, vias, ratsnest

3. **drawTrace(ctx, trace, viewport)**:
   - Set strokeStyle from layer color (same as pads)
   - Set lineWidth from trace.width converted to screen
   - Set lineCap = 'round' for proper trace ends
   - Draw polyline through all segments
   - If locked, add subtle pattern (optional, dashed line)

4. **drawVia(ctx, via, viewport)**:
   - Draw filled circle at via.outer_diameter (copper color)
   - Draw drill hole (board background color)
   - Via color: use special via color or blend top/bottom

5. **drawRatsnest(ctx, ratsnest, viewport)**:
   - Draw thin dashed line from start to end
   - Color: yellow/orange (high visibility)
   - Very thin (1px) regardless of zoom

6. **Update renderBoard()**:
   - After rendering pads, before selection:
     - Draw traces (by layer, respecting visibility)
     - Draw vias (always visible if any copper layer visible)
     - Draw ratsnest (if ratsnest layer visible)

7. **Layer ordering**:
   - Bottom copper traces
   - Top copper traces
   - Vias (on top of traces)
   - Ratsnest (on top of everything)
  </action>
  <verify>
    npm run build in viewer/
    Manual visual check of trace rendering
  </verify>
  <done>
    Traces render with proper width and layer colors.
    Vias render with drill holes.
    Visual test shows traces correctly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Ratsnest Layer Toggle</name>
  <files>
    viewer/src/layers.ts
    viewer/index.html
  </files>
  <action>
Add ratsnest as toggleable layer:

1. **Update LAYER_COLORS**:
   - Add 'Ratsnest' layer with distinctive color (e.g., #FFD700 yellow)

2. **Update LayerState**:
   - Add ratsnest visibility (default: true)

3. **Update RenderState** if needed:
   - showRatsnest: boolean

4. **Update index.html**:
   - Add checkbox for Ratsnest in layer panel
   - Style similar to other layer toggles

5. **Update main.ts**:
   - Connect ratsnest checkbox to render state
   - Respect visibility in renderBoard()

6. **Ratsnest styling**:
   - Thin dashed line (1px, [5, 3] dash pattern)
   - High contrast color for visibility
   - Optional: different color per net

7. **Update MockPcbEngine** for testing:
   - Add sample traces and ratsnest to mock data
  </action>
  <verify>
    npm run build in viewer/
    Manual check: toggle ratsnest visibility
  </verify>
  <done>
    Ratsnest layer toggle in UI.
    Ratsnest hidden when toggled off.
    Ratsnest visible and clearly distinguishable.
  </done>
</task>

</tasks>

<verification>
- `cargo build -p cypcb-render` succeeds
- `npm run build` in viewer/ succeeds
- Visual check: traces render at correct width
- Visual check: vias show drill holes
- Visual check: ratsnest toggles on/off
</verification>

<success_criteria>
1. Traces render with actual width (not just lines)
2. Traces use correct layer colors (red=top, blue=bottom)
3. Vias render with copper ring and drill hole
4. Ratsnest shows unrouted connections
5. Ratsnest can be toggled in layer controls
</success_criteria>

<output>
After completion, create `.planning/phases/05-intelligence/05-08-SUMMARY.md`
</output>
