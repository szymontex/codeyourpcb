---
phase: 08-file-picker
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can click Open button to open file picker"
    - "Loading .cypcb file displays board with components"
    - "Loading .ses file shows routed traces"
    - "Drag & drop .cypcb file onto viewer loads it"
    - "Works without WebSocket/backend server"
---

<objective>
Human verification of file picker functionality

Purpose: Confirm all file picker features work correctly in the browser
Output: Verified working file picker for Phase 8 completion
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-file-picker/08-01-SUMMARY.md
@.planning/phases/08-file-picker/08-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    File picker with Open button and drag-drop support for loading .cypcb and .ses files
  </what-built>
  <how-to-verify>
    1. Start the viewer: `cd viewer && npm run dev`
    2. Open browser to http://localhost:5173

    **Test 1: Open button file picker**
    - Click "Open" button in toolbar
    - File dialog should appear with .cypcb,.ses filter
    - Select examples/routing-test.cypcb
    - Board should display with R1, R2, C1 components
    - Status should show "Loaded routing-test.cypcb"

    **Test 2: Load .ses routes**
    - Click "Open" button again
    - Select examples/routing-test.ses
    - Traces should appear connecting the components
    - Status should show "Loaded routes from routing-test.ses"

    **Test 3: Drag & drop .cypcb**
    - Drag examples/blink.cypcb from file manager onto canvas
    - Canvas should show green dashed outline during drag
    - "Drop .cypcb or .ses file" hint should appear
    - Drop file - new board should load
    - LED1 and R1 components visible

    **Test 4: Error handling**
    - With no board loaded, try loading a .ses file
    - Status should show "Load a .cypcb file first"

    **Test 5: Re-select same file**
    - Load routing-test.cypcb
    - Click Open, select routing-test.cypcb again
    - Should reload (tests input.value reset)

    **Test 6: No backend required**
    - Stop any dev server WebSocket
    - Refresh page
    - File picker should still work (pure client-side)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all tests pass, or describe any issues
  </resume-signal>
</task>

</tasks>

<verification>
Human verification confirms:
1. Open button works
2. .cypcb files load and display
3. .ses files load routes
4. Drag & drop works with visual feedback
5. Error handling for wrong order
6. Works without backend
</verification>

<success_criteria>
- FP-01: File picker UI to select .cypcb source files (VERIFIED)
- FP-02: Load corresponding .ses routing files (VERIFIED)
- FP-03: Drag & drop support for files (VERIFIED)
</success_criteria>

<output>
After completion, create `.planning/phases/08-file-picker/08-03-SUMMARY.md`
</output>
