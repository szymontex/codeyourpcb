# Rust build artifacts
/target
*.rlib
*.o

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# OS files
.DS_Store
Thumbs.db

# Generated Tree-sitter parser files
crates/cypcb-parser/grammar/src/
crates/cypcb-parser/grammar/node_modules/
crates/cypcb-parser/grammar/package-lock.json

# Environment files
.env
.env.*
!.env.example

# Local cargo config (machine-specific)
.cargo/config.toml

# FreeRouting JARs (download separately)
freerouting*.jar

# Runtime / generated artifacts
logs/
output/
export-test/
.bg-shell/
.artifacts/

# Legacy planning (migrated to .gsd/)
.planning/

# Local research/notes
TODO.md

# Node.js
node_modules/
.next/
dist/
build/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Misc
*.log
coverage/
.cache/
tmp/
vendor/

# ── GSD agent workspace ──
.gsd/activity/
.gsd/runtime/
.gsd/worktrees/
.gsd/auto.lock
.gsd/metrics.json
.gsd/STATE.md
.gsd/completed-units.json
.gsd/forensics/
.gsd/parallel/
.gsd/gsd.db
.gsd/DISCUSSION-MANIFEST.json
.gsd/milestones/**/*-CONTINUE.md
.gsd/milestones/**/continue.md

# Reference checkouts of other projects. They were committed as submodule
# pointers with no .gitmodules entry, so a fresh clone could not resolve them
# and `git submodule update` failed outright. Nothing here imports them.
viewer/svg-pcb/
viewer/circuitron/

# Playwright output: screenshots, traces and the last-run marker. Regenerated on
# every run, 42MB of it, and it made every test run show up as a dirty tree.
viewer/test-results/

# Router and exporter output: what the tools write beside the design they read.
#
# The `.dsn`, `.ses` and `.routes` files for three examples were tracked, and
# running `route` on any of them rewrote the lot - so they arrived in whatever
# commit came next, twice, one of which needed a commit of its own to put one
# back. Nothing reads them; they are what the tool produces, not what it is
# given.
*.routed.cypcb
*.dsn
*.ses
*.routes

