[package]
name = "cypcb-rules"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "PCB design rules, signal classification, stackup definitions, and routing rule traits"

[dependencies]
cypcb-core = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
