[package]
name = "cypcb-kicad"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "KiCad file format import for CodeYourPCB"

[dependencies]
cypcb-core = { path = "../cypcb-core" }
cypcb-world = { path = "../cypcb-world", default-features = false }
cypcb-router = { path = "../cypcb-router" }
serde = { workspace = true }
# The project file this crate writes beside a board is JSON, and the house's
# name rides in it under a key of this project's own.
serde_json = { workspace = true }
kicad_parse_gen = "0.7"
symbolic_expressions = "0.4"
thiserror.workspace = true
walkdir = "2"

[dev-dependencies]
cypcb-autoroute = { path = "../cypcb-autoroute" }
tempfile = "3"
